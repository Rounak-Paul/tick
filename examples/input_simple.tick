func main() : int {
    var name : string = input("What is your name? ");
    println(format("Hello, {}!", name));
    
    var age_str : string = input("How old are you? ");
    var age : int = str_to_int(age_str);
    println(format("In 10 years, you will be {} years old.", age + 10));
    
    return 0;
}
