// Deterministic Sequential Processing
// Shows sequential function execution with state

func main() {
  let i: int = 0;
  let doubled: int = i * 2;
  let result: int = doubled + 100;
  
  println(i);
  println(doubled);
  println(result);
}
