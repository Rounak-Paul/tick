// DSP-like processing

func main() {
  static i: int = 0;
  static pass_through: int = 0;
  
  i = i + 1;
  
  if (pass_through == 0) {
    println(100);
  }
  pass_through = 1;
  if (pass_through > 0) {
    pass_through = 0;
  }
  
  println(999);
}
