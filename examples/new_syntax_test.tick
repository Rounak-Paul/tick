func add(x : int, y : int) : int {
    var result : int = x + y;
    return result;
}

func main() : int {
    var x : int = 10;
    var y : int = 20;
    var sum : int = add(x, y);
    println(format("Sum: {}", sum));
    return 0;
}
