// Clock Frequency Comparison Concept (now simplified)
// Demonstrates sequential counter operations

func main() {
  static i: int = 0;
  static j: int = 0;
  
  i = i + 1;
  j = j + 1;
  
  println(i);
  println(j);
}
