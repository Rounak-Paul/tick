// Control flow: if/else
func max(int a, int b) -> int {
  if (a > b) {
    return a;
  } else {
    return b;
  }
}

func main() {
  let result: int = max(15, 10);
  println(result);  // Output: 15
}
