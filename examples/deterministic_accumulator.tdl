// Deterministic Accumulator
// Shows deterministic state management with static variables

func main() {
  static counter: int = 0;
  static sum: int = 0;
  
  counter = counter + 1;
  sum = sum + 10;
  
  println(counter);
  println(sum);
}
