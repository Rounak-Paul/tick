// General-purpose: Calculate Fibonacci numbers
func fibonacci(int n) -> int {
  if (n <= 1) {
    return n;
  }
  return fibonacci(n - 1) + fibonacci(n - 2);
}

func main() {
  let result: int = fibonacci(10);
  println(result);
}
